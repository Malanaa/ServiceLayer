````markdown
# System Architecture

Top-level architecture notes for the ServiceLayer project. See per-service docs for detail.

Services included:
- storage-service (9001)
- user-service (8080)
- gateway-service
- payment-service (stub)

Diagrams are available in `diagrams/` (PlantUML and Mermaid sources).

````
